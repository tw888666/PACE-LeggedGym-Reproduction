# Stage2 训练完成与原生警告审计

本次检查确认正式运行记录为 completed（完成），全局更新 0–29999 共 30000 条，最终检查点 `gpt-model-30000.pt` 存在。其更新索引为 30000，熵状态最后更新为 29999，能耗符号 -1、权重 -0.00016、半衰期 500 与决议一致，模型张量均有限。

## 训练有效性问题

完整训练日志 `artifacts/ppo/gpt-stage2-pace-v2-seed0.log` 第 30039、30042 行出现 PhysX（物理仿真引擎）原生警告：foundLostAggregatePairsCapacity（聚合碰撞候选缓冲容量）不足，分别要求 15713824 和 15017855，否则模拟将遗漏交互。

警告文本在最后一条 Python 更新记录之后才输出，但同一位置也刷新了进程启动时的原生扩展加载消息，说明原生输出有缓冲。没有逐条原生时间戳，无法将警告定位到具体更新，不能认定它只发生在退出析构时；也不能证明此前任何里程碑检查点均未受影响。

因此状态为：**训练程序完成，但训练仿真有效性存疑，不能当作干净的 Stage2 科学复现结果。** 不覆盖、修饰或删除原始训练产物；不根据表现换用中间最佳检查点，不擅自更改训练参数或启动重训。

## 本次最终模型评估

继续按授权评估原始最终模型，以了解它实际学到的行为。使用同一事前样本表、终止规则、成功阈值、确定性均值、冻结归一化、51 个定量组及 48 段固定视频，输出至 `artifacts/final-evaluation/gpt-stage2-pace-v2-v1`。每组显式传入 Stage2 最终检查点，评估采用已修复的分离起点布局，并在子进程退出后审核原生日志。

评估自身通过日志审核，不会反过来证明训练过程有效。最终任务、控制、能耗和步态比较必须伴随此限制，不据此宣布 Stage2 复现成功或开放 Stage3。

## 执行与自动汇总

正式评估已启动，定量组分别由 GPU2–5 的 `gpt-stage2-final-q2/q3/q4/q5` tmux（可断线终端）会话领取，GPU1 的 `gpt-stage2-final-v1` 执行固定视频。GPU0 当前有其他大型任务，未修改或停止其工作。每个评估子进程退出后仍须通过原生日志审核。

`gpt-stage2-final-report` 会话等待全部 51 个定量组及 16 个视频组完成后，自动生成 Stage2 专用报告和 Stage1 同口径对照，避免套用 Stage1 中断历史。训练有效性问题同时写入机器可读汇总和正文，不能只留在旁注。当前进度见评估目录 `gpt-评估总体进度.json`。

六个固定步态回放独立运行于 `artifacts/gait-audit/gpt-stage2-model30000-v1`；完成后 `gpt-stage2-gait-report` 会话自动生成 Stage2 步态与 Stage1 对照报告。训练趋势图已生成，注明物理有效性存疑，不将原先 Stage1 第 300 次中断标签套到 Stage2。

完成复核：2026-09-11 已确认 51 个定量组、16 个视频组全部通过原生日志和归一化冻结检查，48 段固定视频覆盖完整，六个步态回放与报告已完成。模型数值任务阈值达到，但训练有效性警告仍未解除，未开放 Stage3、未改参或重训。

后续定位：无训练的原始4096环境复现了相同15713824候选容量请求，细化事件日志将首次警告定位到 prepare_sim（准备仿真）阶段，早于首次重置和策略推理。原始布局首次策略步还有第二条警告。它证明相同问题可在初始化触发，不能逐条确定历史训练缓冲日志的实际产生时刻。Stage1源快照的创建逻辑相同，故同样存在未排除的初始化风险。详见 [物理容量排查结果](gpt-Stage2-物理容量排查结果.md)。
