# Stage1 PACE v2 检查点工程中断记录

日期：2026-09-08。在完成最终评估预注册后，仅查看运行状态发现正式训练已失败；随后只读取错误与检查点元数据，未据训练性能调整参数。

失败位置：完成 300 次 PPO 更新后的检查点校验。`copy.deepcopy(model)` 试图复制 PPO 缓存的含非叶张量的动作分布，PyTorch 拒绝复制。该错误属于新增检查点校验器的工程错误，不是动作、奖励或学习健康失败。
磁盘 `gpt-model-300.tmp` 已完整写出：迭代索引 300、熵最后更新 299、优化器状态存在，模型及统计有限。另保留原内容为 `gpt-interrupted-300.pt`，保留首次运行日志和运行记录。

修复：只复制 actor/critic（策略/价值网络）推理模块并从检查点加载，不复制 PPO 的临时分布缓存。通过真实 PPO 核心的合成张量更新后保存/恢复测试；未追加仿真验证训练。
恢复从第 300 次更新检查点继续，保留权重、优化器、自适应学习率、归一化统计、熵和下一次更新索引；不从头重训，不筛选检查点。

必须报告的边界：模拟器状态和 RNG（随机数发生器）未保存在原检查点，因此该次运行是工程中断后的分段续训，不是逐位一致的无中断轨迹。新环境首次观测计入一次独立批次，归一化计数会额外增加 4096；该处理沿用新返回观测计数一次的规则，日志使用分段起始计数核验。物理子步监测计数也按新分段重新开始，训练更新索引保持全局连续。
训练超参数、动作映射、奖励、归一化公式和熵调度不变。最终报告需包含此次中断，不能把分段边界掩盖为原样连续训练；未来跨阶段比较也须披露。
