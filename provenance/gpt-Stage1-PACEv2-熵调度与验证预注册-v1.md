# 熵调度与验证预注册 v1

日期：2026-09-07；标识 `ENTROPY_VALIDATION_RECONSTRUCTION_V1`。
按用户指定的调度几何预注册，不扫描、不依据运动表现选值。

```math
\eta=\frac{\operatorname{atanh}(0.8)}{10000}=0.00010986122886681098
```

沿用论文起始渐近值 0.002、最终渐近值 0.0005、转折点 20000。令 30000 时过渡因子等于 0.1，
对应系数 0.00065；20000 时为 0.00125；0 时为约 0.0019817073170731707。
η 来源为 `REPRODUCTION_ASSUMPTION`（复现假设），`paper_exact=false`、`performance_based_selection=false`。
初始和最终是渐近端点，不强行覆盖有限迭代处的公式值。

迭代编号采用从 0 起的 PPO 更新索引：第一个更新使用 E(0)，保存的 `iter` 是下一个尚未执行的更新索引。
完成索引 12344 的更新后保存 iter=12345，恢复后第一个更新使用 E(12345)。调度记录的 last_iteration=iter-1。
完整 30000 次更新使用索引 0..29999；E(30000) 是训练结束边界上的几何条件。
任务课程与熵调度使用同一更新索引；验证只截取正式调度的前段，不压缩 20k 转折点。

静态和恢复测试通过后，只允许 PACE_V2_VALIDATION（短程验证），PACE_V2_FORMAL（正式训练）保持禁止。
V0：64 环境、10 次迭代、seed=123、plane（平地）、SMOKE/NON-EXPERIMENTAL（冒烟/非实验），
检查构造、归一化、公式（9）、有限奖励和 PPO 更新、熵注入与完整检查点恢复。
V0 通过后 V1：4096 环境、300 次迭代、seed=0、plane、完整表 8 PPO 配置。
所有检查点为验证产物，不标为正式模型。GPU 任务在 tmux（可断线恢复终端）中运行。

V1 先核验协议，再记录奖励、回合长度、速度误差、策略均值/标准差、原始动作、力矩饱和与关节限位活动。
不以 300 次迭代必须走稳为通过条件；不因动作发散临时添加惩罚或改变已冻结配置。
只有有限性和入口协议检查通过才运行后续阶段；正式门禁本轮始终关闭。

执行后记录：V0/V1 均完成；结果见[验证入口与短训审查](gpt-Stage1-PACEv2-验证入口与短训审查.md)。本节为执行后追加，不改变上述预注册选择。正式训练仍关闭。
