# 物理容量修复成对重训执行记录

2026-09-11，用户在确认 GPU1 资源可用、建议依次重训 Stage1 → Stage2 后回复“可以，继续”，授权执行成对重训。

两个阶段均使用 GPU1（A100 80GB），从头 seed0、4096 混合地形、30000 次更新。沿用原训练协议及每 500 次保存检查点设置。Stage2 不继承 Stage1 权重，继续使用已批准的负势能解释和能耗课程。修复仅包括构造期分离与接触对容量提升至 33554432，总缓冲倍率保持 5；修复遗漏交互可能改变后续仿真轨迹和权重。

队列脚本：[顺序执行脚本](../artifacts/ppo/gpt-容量修复成对重训队列.sh)。tmux（可断线终端）会话名为 `gpt-capacity-r2-gpu1`。Stage1 的监督器确认正常完成 30000 次后才启动 Stage2；任一阶段失败即退出队列。原生遗漏交互警告会触发监督器停止对应训练。

输出目录分别为 `artifacts/ppo/gpt-pace-v2-formal-stage1-seed0-capacity-r2` 和 `artifacts/ppo/gpt-pace-v2-formal-stage2-seed0-capacity-r2`。历史模型和结果保留。实时状态以各目录的 `gpt-运行记录.json`、`gpt-逐迭代指标.jsonl` 及目录旁的原生监督日志为准。启动记录不代表训练已经完成。

执行命令：

```bash
tmux new-session -d -s gpt-capacity-r2-gpu1 -c /home/xy.chen/tw/PACE-LeggedGym-Reproduction 'bash artifacts/ppo/gpt-容量修复成对重训队列.sh > artifacts/ppo/gpt-容量修复成对重训队列.log 2>&1'
```

启动核验：2026-09-11 10:27:31（北京时间）队列启动。Stage1 已在 GPU1 完成首次 PPO（近端策略优化）更新，运行记录为 `running`；首轮迭代编号 0，熵系数 0.0019817073170731708，演员和评论家归一化统计样本数均为 102400。4096 环境、seed0、30000 次目标和容量修复配置均由实际运行记录确认。Stage2 正在队列中等待，尚未启动。

完成核验（2026-09-13 11:32，北京时间）：两个阶段均已完成 30000 次更新。Stage1 于 2026-09-12 03:12:06 完成，Stage2 于 2026-09-13 01:43:12 完成；队列于 01:43:18 正常结束。两个原生监督器均记录退出码 0、`native_error=null`；复查原生日志未发现遗漏交互、CUDA 错误、显存不足或 Python 异常回溯。两个输出目录均存在 `gpt-model-30000.pt` 与 `gpt-model-final.pt`。本次是运行记录与文件存在性核验，尚未进行修复后模型的最终科学评估。

最后一次训练更新的描述性指标：Stage1 / Stage2 的策略标准差均值为 5.81145 / 0.11017，原始策略均值绝对值为 31.66520 / 0.93025，力矩饱和率为 77.77046% / 2.41759%。这不是统一评估结果，也不用于调参或替代最终检查点。

下一步：复用原最终评估协议对两个 30000 次模型进行成对比较。Stage3 尚未启动。
