# PACE v2 正式训练入口审查

日期：2026-09-08。状态：准备正式启动方案，尚未批准或启动正式训练。

平地 V1 的 300 次更新已通过协议和恢复检查，结果见[短训审查](gpt-Stage1-PACEv2-验证入口与短训审查.md)。它支持继续推进，但不证明混合地形上的最终表现或长期稳定性。
本次不改变动作、归一化、奖励、公式（9）或熵调度的预注册值。

## 实际入口缺口与修复

旧 `pace_stage1.train --flat-seed0` 仍是历史平地消融入口，不能用于本次正式 PACE v2 重建。
已增加 `pace_stage1.pace_v2_formal`，复用 V1 的更新循环、rsl_rl 1.0.2 PPO、独立 actor/critic（策略/价值网络）归一化和完整检查点保存语义。
只读指标提取为共享混入类；原 V0/V1 的默认平地配置保持原值。
正式入口在创建输出目录或模拟器之前检查正式门禁。实际调用确认门禁关闭时抛出禁止启动错误，输出目录未创建。

## 拟启动配置

| 项目 | 值 |
|---|---|
| 阶段 | Stage1 Task-only（仅任务奖励），能耗项关闭 |
| 训练规模 | 4096 环境，30000 次 PPO 更新 |
| 随机种子 | 0 |
| 初始化 | 从头初始化；不使用 V1 检查点热启动 |
| 地形 | trimesh（三角网格），现有混合地形课程 |
| 地形类型比例 | 光滑坡 0.1、粗糙坡 0.1、上楼梯 0.35、下楼梯 0.25、离散障碍 0.2 |
| 地形网格 | 10 行 × 20 列，每块 8 m × 8 m，初始最高等级 5 |
| 熵调度 | 原预注册几何，更新索引 0..29999 |
| 保存 | 每 500 次更新及最终保存模型、优化器、两组归一化统计、调度与下一次更新索引 |
| 运行记录 | 每次更新记录奖励、动作、标准差、限位与力矩饱和；保留源代码和来源文档副本 |

保留当前配置中的摩擦随机化和每 15 秒外推扰动；没有为获得更好表现关闭这些项。
混合地形相对平地会改变采样数据，因此正式训练产生不同权重是预期结果。本次只准备入口，没有训练正式权重。
现入口仅支持从头开始正式运行；检查点具备模型/优化器恢复能力，但未实现正式命令行续训功能，也不声称能恢复模拟器或完整随机轨迹。

批准后拟用以下单行命令启动；当前门禁关闭，直接执行会被拒绝：

```bash
tmux new-session -d -s gpt-pace-v2-formal-stage1 'cd /home/xy.chen/tw/PACE-LeggedGym-Reproduction && CUDA_VISIBLE_DEVICES=2 conda run --no-capture-output -n bruce_gym env PYTHONPATH=src python -m pace_stage1.pace_v2_formal --output artifacts/ppo/gpt-pace-v2-formal-stage1-seed0 > /tmp/gpt-pace-v2-formal-stage1.log 2>&1'
```

GPU 2 为 V100。tmux（可断线恢复终端）保证 SSH 断开后训练继续。该命令只在正式批准并更新门禁之后使用；没有自动从验证通过转为正式批准。

## 验证与结论

混合地形集成冒烟已通过：64 环境、seed 123、10 次更新，GPU 2 V100，约 51.4 秒（含地形构造和恢复检查）；协议检查 PASS（通过），恢复后的归一化输入、确定性动作与价值输出逐位相等，下一次更新索引为 10。产物位于 `artifacts/validation/gpt-pace-v2-20260908-mixed-v0`。33 项 PACE v2 测试通过。这只证明混合地形路径可以运行，不评价 10 次更新的学习表现。

原始动作峰值 14.81 和策略均值峰值 14.00 仍作为后续监测关注项，不据此改参数。
正式授权需单独确认，依据是用户此前明确要求“VALIDATION 开放、FORMAL 继续关闭，再决定是否开放正式训练”，不是新增技能或工具的审批要求。
